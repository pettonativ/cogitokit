# Agent Instructions (cogitokit)

Agent registry, routing, boundaries and rule priorities: **`.agent/rules/AGENTS.md`**.

Global behavior and classifier: **`.agent/rules/CORE.md`**.

Personas and operational workflows: **`.agent/agents/*.md`** and **`.agent/workflows/`**.

Skills exposed to Cursor: **`.cursor/skills/`** (generate/update with `python3 scripts/sync_cursor_workspace.py`).
